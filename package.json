{
  "name": "css-responsivo-bulma",
  "version": "1.0.0",
  "description": "Projeto para a matéria Desenvolvimento Web 1, pesquisa e projeto sobre a biblioteca CSS Bulma",
  "scripts": {
    "dev": "parcel serve index.html --dist-dir dev --open --port 3000",
    "build": "parcel build index.html --dist-dir docs --no-cache  --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "bulma": "^1.0.0"
  }
}
